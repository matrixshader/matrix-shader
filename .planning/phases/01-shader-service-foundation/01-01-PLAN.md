---
phase: 01-shader-service-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MatrixShader/src/MatrixShader.Core/Models/ShaderConfig.cs
  - MatrixShader/src/MatrixShader.Core/Constants/ColorPresets.cs
autonomous: true

must_haves:
  truths:
    - "ShaderConfig validation accepts values PowerShell allows (Speed up to 3.0, Glow from 0.2)"
    - "ShaderConfig defaults match actual HLSL shader defaults"
    - "Color presets RGB values exactly match PowerShell matrix_control.ps1"
  artifacts:
    - path: "MatrixShader/src/MatrixShader.Core/Models/ShaderConfig.cs"
      provides: "Shader parameter model with correct validation ranges"
      contains: "Speed >= 0.1f && Speed <= 3f"
    - path: "MatrixShader/src/MatrixShader.Core/Constants/ColorPresets.cs"
      provides: "6 color presets matching PowerShell"
      exports: ["Green", "Cyan", "Red", "Purple", "Gold", "Teal"]
  key_links:
    - from: "ShaderConfig.IsValid()"
      to: "PowerShell Adj function ranges"
      via: "matching min/max values"
      pattern: "Speed >= 0\\.1f && Speed <= 3f"
---

<objective>
Fix ShaderConfig validation ranges and defaults to match the working PowerShell implementation.

Purpose: ShaderConfig is the data model used throughout the application. If validation ranges are wrong, users can't set values that PowerShell allows (e.g., Speed=2.5). If defaults are wrong, new configs won't match shader behavior.

Output: Updated ShaderConfig.cs with correct validation ranges matching matrix_control.ps1 Adj function.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shader-service-foundation/01-RESEARCH.md

Reference files:
- PowerShell source: C:\Users\ehome\documents\matrix\matrix_control.ps1 (lines 1103-1119 for Adj function ranges)
- Existing C# model: C:\Users\ehome\documents\matrix\MatrixShader\src\MatrixShader.Core\Models\ShaderConfig.cs
- Existing presets: C:\Users\ehome\documents\matrix\MatrixShader\src\MatrixShader.Core\Constants\ColorPresets.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ShaderConfig validation ranges</name>
  <files>MatrixShader/src/MatrixShader.Core/Models/ShaderConfig.cs</files>
  <action>
Update the IsValid() method to match PowerShell matrix_control.ps1 Adj function ranges:

| Parameter | Current C# | Correct (PowerShell) |
|-----------|------------|---------------------|
| R, G, B   | 0.0 - 1.0  | 0.0 - 1.0 (correct) |
| Speed     | 0.1 - 2.0  | 0.1 - 3.0 (FIX)     |
| Glow      | 0.0 - 2.0  | 0.2 - 3.0 (FIX)     |
| Width     | 5.0 - 20.0 | 6.0 - 20.0 (FIX)    |
| Trail     | 1.0 - 20.0 | 4.0 - 15.0 (FIX)    |
| Density   | 0.1 - 1.0  | 0.2 - 1.0 (FIX)     |

Also update the XML doc comments to reflect correct ranges.

DO NOT change the property defaults - those match the shader file defaults and are correct.
  </action>
  <verify>Build the project: `dotnet build MatrixShader/src/MatrixShader.Core/MatrixShader.Core.csproj`</verify>
  <done>ShaderConfig.IsValid() returns true for Speed=2.5, Glow=2.5, Width=6.0, Trail=14.0, Density=0.2</done>
</task>

<task type="auto">
  <name>Task 2: Add Clamp method to ShaderConfig</name>
  <files>MatrixShader/src/MatrixShader.Core/Models/ShaderConfig.cs</files>
  <action>
Add a Clamp() method that returns a new ShaderConfig with all values clamped to valid ranges. This is used when reading potentially corrupted shader files to ensure values are usable.

```csharp
/// <summary>
/// Returns a new config with all values clamped to valid ranges.
/// </summary>
public ShaderConfig Clamp() => this with
{
    R = Math.Clamp(R, 0f, 1f),
    G = Math.Clamp(G, 0f, 1f),
    B = Math.Clamp(B, 0f, 1f),
    Speed = Math.Clamp(Speed, 0.1f, 3f),
    Glow = Math.Clamp(Glow, 0.2f, 3f),
    Width = Math.Clamp(Width, 6f, 20f),
    Trail = Math.Clamp(Trail, 4f, 15f),
    Density = Math.Clamp(Density, 0.2f, 1f)
    // Layer bools don't need clamping
};
```

Also add a static Default property for clarity:

```csharp
/// <summary>
/// Default configuration matching Matrix-1.hlsl defaults.
/// </summary>
public static ShaderConfig Default => new();
```
  </action>
  <verify>Build the project: `dotnet build MatrixShader/src/MatrixShader.Core/MatrixShader.Core.csproj`</verify>
  <done>ShaderConfig has Clamp() method that constrains Speed=5.0 to 3.0, and Default property returns instance with classic green defaults</done>
</task>

<task type="auto">
  <name>Task 3: Verify ColorPresets match PowerShell</name>
  <files>MatrixShader/src/MatrixShader.Core/Constants/ColorPresets.cs</files>
  <action>
Compare ColorPresets.cs values against matrix_control.ps1 lines 1095-1100:

PowerShell color values:
1. Green:  R=0.0, G=1.0, B=0.3
2. Cyan:   R=0.0, G=0.6, B=1.0
3. Red:    R=1.0, G=0.1, B=0.1
4. Purple: R=0.7, G=0.0, B=1.0
5. Gold:   R=1.0, G=0.7, B=0.0
6. Teal:   R=0.0, G=0.9, B=0.9

Current ColorPresets.cs values should already match (they were implemented correctly). Verify and document any discrepancies.

If all values match, no changes needed - just verify and document in the summary.
  </action>
  <verify>Read ColorPresets.cs and compare values manually against the PowerShell list above</verify>
  <done>All 6 color presets verified to match PowerShell values exactly, or corrected if discrepancies found</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `dotnet build MatrixShader/src/MatrixShader.Core/MatrixShader.Core.csproj` succeeds with no errors
2. ShaderConfig.IsValid() returns true for boundary values: Speed=3.0, Glow=0.2, Width=6.0, Trail=4.0, Density=0.2
3. new ShaderConfig().IsValid() returns true (defaults are valid)
4. ShaderConfig.Default.Clamp() equals ShaderConfig.Default (defaults don't need clamping)
</verification>

<success_criteria>
- ShaderConfig validation ranges match PowerShell exactly
- Clamp() method constrains out-of-range values
- ColorPresets verified against PowerShell source
- Project builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01-shader-service-foundation/01-01-SUMMARY.md`
</output>
