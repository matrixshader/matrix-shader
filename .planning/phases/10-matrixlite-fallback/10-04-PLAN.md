---
phase: 10-matrixlite-fallback
plan: 04
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - MatrixShader/src/MatrixShader.Cli/MatrixLite/Program.cs
  - MatrixShader/src/MatrixShader.Cli/MatrixLite/MatrixShader.Cli.MatrixLite.csproj
autonomous: true

must_haves:
  truths:
    - "matrixlite.exe is a standalone CLI application"
    - "matrixlite.exe launches directly into FallbackMenu"
    - "matrixlite.exe has Native AOT publishing configuration"
    - "matrixlite.exe works in any terminal with ANSI support"
  artifacts:
    - path: "MatrixShader/src/MatrixShader.Cli/MatrixLite/Program.cs"
      provides: "Standalone MatrixLite entry point"
      contains: "FallbackMenu"
    - path: "MatrixShader/src/MatrixShader.Cli/MatrixLite/MatrixShader.Cli.MatrixLite.csproj"
      provides: "Project configuration"
      contains: "PublishAot"
  key_links:
    - from: "MatrixLite Program.cs"
      to: "MatrixShader.Lite"
      via: "FallbackMenu"
      pattern: "FallbackMenu"
---

<objective>
Create a standalone matrixlite.exe CLI that launches directly into the text-based Matrix rain effect without any environment detection.

Purpose: Provide a dedicated executable for users who want text-based Matrix rain regardless of terminal
Output: matrixlite.exe that launches FallbackMenu immediately with theatrical intro
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@MatrixShader/src/MatrixShader.Cli/Redpill/Program.cs (reference for CLI structure)
@MatrixShader/src/MatrixShader.Lite/FallbackMenu.cs
@MatrixShader/src/MatrixShader.Core/Startup/MatrixSplash.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MatrixLite CLI project structure</name>
  <files>
    MatrixShader/src/MatrixShader.Cli/MatrixLite/MatrixShader.Cli.MatrixLite.csproj
  </files>
  <action>
    Create a new directory `MatrixShader/src/MatrixShader.Cli/MatrixLite/` if it doesn't exist.

    Create the project file with Native AOT configuration matching other CLIs:

    ```xml
    <Project Sdk="Microsoft.NET.Sdk">

      <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net8.0-windows</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <AssemblyName>matrixlite</AssemblyName>

        <!-- Native AOT Configuration -->
        <PublishAot>true</PublishAot>
        <PublishSingleFile>true</PublishSingleFile>
        <RuntimeIdentifier>win-x64</RuntimeIdentifier>
        <SelfContained>true</SelfContained>
        <InvariantGlobalization>true</InvariantGlobalization>
        <IlcOptimizationPreference>Size</IlcOptimizationPreference>
      </PropertyGroup>

      <ItemGroup>
        <ProjectReference Include="..\..\MatrixShader.Core\MatrixShader.Core.csproj" />
        <ProjectReference Include="..\..\MatrixShader.Lite\MatrixShader.Lite.csproj" />
      </ItemGroup>

    </Project>
    ```
  </action>
  <verify>
    Read the csproj file and confirm it contains all required properties and project references.
  </verify>
  <done>
    MatrixShader.Cli.MatrixLite.csproj created with Native AOT configuration
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MatrixLite Program.cs entry point</name>
  <files>
    MatrixShader/src/MatrixShader.Cli/MatrixLite/Program.cs
  </files>
  <action>
    Create the main entry point for matrixlite.exe:

    ```csharp
    using System.Text;
    using MatrixShader.Core.Constants;
    using MatrixShader.Core.Helpers;
    using MatrixShader.Core.Startup;
    using MatrixShader.Lite;

    namespace MatrixShader.Cli.MatrixLite;

    /// <summary>
    /// MatrixLite - Standalone text-based Matrix rain effect.
    /// Works in any terminal with ANSI and Unicode support.
    /// </summary>
    public static class Program
    {
        public static async Task<int> Main(string[] args)
        {
            // Handle help
            if (args.Contains("--help") || args.Contains("-h"))
            {
                ShowHelp();
                return 0;
            }

            try
            {
                Console.OutputEncoding = Encoding.UTF8;

                // Skip splash if --quiet flag
                if (!args.Contains("--quiet") && !args.Contains("-q"))
                {
                    await ShowIntro();
                }

                // Launch directly into FallbackMenu
                var menu = new FallbackMenu();
                await menu.RunAsync(CancellationToken.None);

                return 0;
            }
            catch (Exception ex)
            {
                Console.WriteLine($"\x1b[31mError: {ex.Message}\x1b[0m");
                return 1;
            }
        }

        private static async Task ShowIntro()
        {
            Console.Clear();
            Console.WriteLine();

            // Simple Matrix-style intro
            Console.Write("\x1b[32m");
            await TypewriterAsync(" Wake up, Neo...", 80);
            await Task.Delay(500);

            await TypewriterAsync(" The Matrix has you...", 60);
            await Task.Delay(500);

            Console.WriteLine();
            Console.WriteLine(" MATRIXLITE - Text-based Matrix Rain");
            Console.WriteLine(" ------------------------------------");
            Console.Write("\x1b[0m");

            await Task.Delay(1000);
        }

        private static async Task TypewriterAsync(string text, int charDelayMs)
        {
            foreach (char c in text)
            {
                Console.Write(c);
                await Task.Delay(charDelayMs);
            }
            Console.WriteLine();
        }

        private static void ShowHelp()
        {
            Console.WriteLine();
            Console.WriteLine("\x1b[32m MATRIXLITE - Text-based Matrix Rain\x1b[0m");
            Console.WriteLine();
            Console.WriteLine("\x1b[90m Usage: matrixlite [options]\x1b[0m");
            Console.WriteLine();
            Console.WriteLine("\x1b[90m Options:\x1b[0m");
            Console.WriteLine("\x1b[90m   --help, -h     Show this help message\x1b[0m");
            Console.WriteLine("\x1b[90m   --quiet, -q    Skip intro animation\x1b[0m");
            Console.WriteLine();
            Console.WriteLine("\x1b[90m Controls (during animation):\x1b[0m");
            Console.WriteLine("\x1b[90m   [1-6]          Color presets\x1b[0m");
            Console.WriteLine("\x1b[90m   [E/R]          Speed -/+\x1b[0m");
            Console.WriteLine("\x1b[90m   [D/F]          Density -/+\x1b[0m");
            Console.WriteLine("\x1b[90m   [Enter]        Toggle animation\x1b[0m");
            Console.WriteLine("\x1b[90m   [Q/Escape]     Return to menu / Quit\x1b[0m");
            Console.WriteLine();
        }
    }
    ```
  </action>
  <verify>
    Read the Program.cs file and confirm:
    1. Help flag handling is present
    2. Intro animation is implemented
    3. FallbackMenu is created and RunAsync is awaited
    4. Error handling returns exit code 1
  </verify>
  <done>
    MatrixLite Program.cs created with intro and FallbackMenu launch
  </done>
</task>

<task type="auto">
  <name>Task 3: Add MatrixLite to solution and build</name>
  <files>
    MatrixShader/MatrixShader.sln
  </files>
  <action>
    Add the new MatrixLite project to the solution:

    ```bash
    cd MatrixShader
    dotnet sln add src/MatrixShader.Cli/MatrixLite/MatrixShader.Cli.MatrixLite.csproj
    ```

    Then build the project to verify:

    ```bash
    dotnet build src/MatrixShader.Cli/MatrixLite -c Debug
    ```

    Also verify entire solution builds:

    ```bash
    dotnet build MatrixShader.sln -c Debug
    ```
  </action>
  <verify>
    1. Solution file contains MatrixLite project reference
    2. dotnet build succeeds for MatrixLite project
    3. dotnet build succeeds for entire solution
  </verify>
  <done>
    MatrixLite project added to solution and builds successfully
  </done>
</task>

<task type="auto">
  <name>Task 4: Test Native AOT publish for MatrixLite</name>
  <files>None (verification only)</files>
  <action>
    Publish MatrixLite with Native AOT to verify single-file output:

    ```bash
    cd MatrixShader
    dotnet publish src/MatrixShader.Cli/MatrixLite -c Release
    ```

    Verify:
    1. Publish completes without errors
    2. Single matrixlite.exe file in publish folder
    3. No IL2XXX trim warnings

    Check publish output:
    ```bash
    dir /b src\MatrixShader.Cli\MatrixLite\bin\Release\net8.0-windows\win-x64\publish\
    ```
  </action>
  <verify>
    Publish folder contains matrixlite.exe (and optionally .pdb), no other .dll files.
  </verify>
  <done>
    matrixlite.exe publishes to single-file Native AOT binary
  </done>
</task>

</tasks>

<verification>
- [ ] MatrixLite directory created under MatrixShader.Cli
- [ ] MatrixShader.Cli.MatrixLite.csproj has Native AOT configuration
- [ ] Program.cs handles --help and --quiet flags
- [ ] Program.cs shows intro animation
- [ ] Program.cs launches FallbackMenu.RunAsync
- [ ] Project added to MatrixShader.sln
- [ ] dotnet build succeeds for MatrixLite
- [ ] dotnet build succeeds for entire solution
- [ ] dotnet publish produces single matrixlite.exe
</verification>

<success_criteria>
matrixlite.exe is a standalone Native AOT executable that launches directly into the text-based Matrix rain effect with theatrical intro.
</success_criteria>

<output>
After completion, create `.planning/phases/10-matrixlite-fallback/10-04-SUMMARY.md`
</output>
