---
phase: 10-matrixlite-fallback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MatrixShader/src/MatrixShader.Cli/Bluepill/Program.cs
autonomous: true

must_haves:
  truths:
    - "bluepill.exe detects non-Windows-Terminal environment"
    - "bluepill.exe falls back to FallbackMenu when not in WT"
    - "Lite mode displays text-based Matrix rain via TextMatrixRenderer"
    - "Error handling preserves theatrical UX in Lite mode"
  artifacts:
    - path: "MatrixShader/src/MatrixShader.Cli/Bluepill/Program.cs"
      provides: "Graceful degradation logic"
      contains: "RenderMode.Lite"
    - path: "MatrixShader/src/MatrixShader.Cli/Bluepill/Program.cs"
      provides: "FallbackMenu import"
      contains: "using MatrixShader.Lite"
  key_links:
    - from: "Bluepill Program.cs"
      to: "MatrixShader.Lite"
      via: "FallbackMenu"
      pattern: "FallbackMenu"
    - from: "Bluepill Program.cs"
      to: "EnvironmentService"
      via: "DetectRenderMode"
      pattern: "DetectRenderMode"
---

<objective>
Add graceful degradation to bluepill.exe so it falls back to MatrixLite text rendering when not running in Windows Terminal.

Purpose: Enable bluepill.exe to work in any console environment (CMD, PowerShell, other terminals)
Output: bluepill.exe automatically uses FallbackMenu with TextMatrixRenderer when WT_SESSION is not detected
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@MatrixShader/src/MatrixShader.Cli/Redpill/Program.cs (reference implementation)
@MatrixShader/src/MatrixShader.Lite/FallbackMenu.cs
@MatrixShader/src/MatrixShader.Core/Services/EnvironmentService.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MatrixShader.Lite reference to Bluepill project</name>
  <files>
    MatrixShader/src/MatrixShader.Cli/Bluepill/MatrixShader.Cli.Bluepill.csproj
  </files>
  <action>
    Check if MatrixShader.Lite ProjectReference already exists in the csproj file.

    If not present, add the following ProjectReference:
    ```xml
    <ProjectReference Include="..\..\MatrixShader.Lite\MatrixShader.Lite.csproj" />
    ```

    The reference should be added within the existing ItemGroup that contains other ProjectReferences.
  </action>
  <verify>
    Read the csproj file and confirm MatrixShader.Lite ProjectReference is present.
  </verify>
  <done>
    MatrixShader.Cli.Bluepill.csproj contains ProjectReference to MatrixShader.Lite
  </done>
</task>

<task type="auto">
  <name>Task 2: Add render mode detection to Bluepill Program.cs</name>
  <files>
    MatrixShader/src/MatrixShader.Cli/Bluepill/Program.cs
  </files>
  <action>
    Add `using MatrixShader.Lite;` import at the top of the file.

    After DI setup and before session restore, add render mode detection:

    ```csharp
    var envService = provider.GetRequiredService<EnvironmentService>();
    var mode = envService.DetectRenderMode();

    if (mode == RenderMode.Lite)
    {
        // Lite mode - use text-based Matrix rain
        Console.WriteLine();
        ConsoleHelper.WriteLineMatrixGreen(" LITE MODE - No Windows Terminal detected");
        ConsoleHelper.WriteLineDim(" Running text-based Matrix rain...");
        Console.WriteLine();

        var menu = new FallbackMenu();
        await menu.RunAsync(CancellationToken.None);
        return 0;
    }
    else if (mode == RenderMode.Headless)
    {
        Console.WriteLine("\x1b[31mNo display available. Use --help for options.\x1b[0m");
        return 1;
    }

    // Full mode continues with session restore...
    ```

    Add this check AFTER the CliBootstrap.ShowRandomQuote() call but BEFORE the session restore logic.

    The Full mode path should continue with the existing SessionRestorer logic.
  </action>
  <verify>
    Read the updated Program.cs and confirm:
    1. `using MatrixShader.Lite;` is present
    2. `envService.DetectRenderMode()` is called
    3. Lite mode branch creates FallbackMenu and calls RunAsync
    4. Headless mode returns error
    5. Full mode path proceeds to SessionRestorer
  </verify>
  <done>
    Bluepill Program.cs detects render mode and routes to FallbackMenu for Lite mode
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify Bluepill build and Lite mode path</name>
  <files>None (verification only)</files>
  <action>
    Build the Bluepill project to ensure no compilation errors:

    ```bash
    cd MatrixShader
    dotnet build src/MatrixShader.Cli/Bluepill -c Debug
    ```

    Verify:
    1. Build succeeds with no errors
    2. No new warnings related to the Lite integration

    Optionally test by checking that EnvironmentService.IsWindowsTerminal() returns false when WT_SESSION is not set.
  </action>
  <verify>
    Build output shows success (exit code 0, no errors).
  </verify>
  <done>
    Bluepill builds successfully with Lite mode integration
  </done>
</task>

</tasks>

<verification>
- [ ] MatrixShader.Lite ProjectReference added to Bluepill.csproj
- [ ] `using MatrixShader.Lite;` import present in Program.cs
- [ ] DetectRenderMode() called after DI setup
- [ ] Lite mode branch creates FallbackMenu and awaits RunAsync
- [ ] Headless mode returns error code 1
- [ ] Full mode path still executes SessionRestorer
- [ ] dotnet build succeeds with no errors
</verification>

<success_criteria>
bluepill.exe detects non-Windows-Terminal environments and gracefully falls back to FallbackMenu with text-based Matrix rain instead of failing.
</success_criteria>

<output>
After completion, create `.planning/phases/10-matrixlite-fallback/10-01-SUMMARY.md`
</output>
