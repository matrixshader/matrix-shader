---
phase: 03-windows-api-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MatrixShader/src/MatrixShader.Core/Native/WindowsApi.cs
  - MatrixShader/src/MatrixShader.Core/Models/BorderMargins.cs
  - MatrixShader/src/MatrixShader.Core/Models/WindowRect.cs
autonomous: true

must_haves:
  truths:
    - "DwmGetWindowAttribute P/Invoke declaration compiles without error"
    - "BorderMargins record captures invisible border offsets (Left, Top, Right, Bottom)"
    - "WindowRect.Empty static property returns zero-dimension rectangle"
  artifacts:
    - path: "MatrixShader/src/MatrixShader.Core/Native/WindowsApi.cs"
      provides: "DwmGetWindowAttribute P/Invoke declaration"
      contains: "DwmGetWindowAttribute"
    - path: "MatrixShader/src/MatrixShader.Core/Models/BorderMargins.cs"
      provides: "Border margins record for invisible border tracking"
      exports: ["BorderMargins", "Zero"]
    - path: "MatrixShader/src/MatrixShader.Core/Models/WindowRect.cs"
      provides: "Empty static property"
      contains: "static WindowRect Empty"
  key_links:
    - from: "WindowsApi.cs"
      to: "dwmapi.dll"
      via: "LibraryImport P/Invoke"
      pattern: 'LibraryImport.*dwmapi'
---

<objective>
Add DWM P/Invoke declarations and BorderMargins model for Windows 10/11 invisible border detection.

Purpose: Windows 10/11 have invisible resize borders (7px on left/right/bottom) that GetWindowRect includes but aren't visible. DwmGetWindowAttribute with DWMWA_EXTENDED_FRAME_BOUNDS is required for pixel-perfect positioning.

Output: DWM P/Invoke in WindowsApi.cs, BorderMargins model, WindowRect.Empty property
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-windows-api-layer/03-RESEARCH.md

Reference files:
@MatrixShader/src/MatrixShader.Core/Native/WindowsApi.cs
@MatrixShader/src/MatrixShader.Core/Models/WindowRect.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BorderMargins model</name>
  <files>MatrixShader/src/MatrixShader.Core/Models/BorderMargins.cs</files>
  <action>
Create BorderMargins.cs as a record with:
- Left, Top, Right, Bottom int properties (init)
- Static Zero property returning all zeros
- Summary comments explaining this represents invisible window borders

Use record type for immutability, matching WindowRect pattern.
  </action>
  <verify>File exists and compiles: `dotnet build MatrixShader/src/MatrixShader.Core`</verify>
  <done>BorderMargins.cs exists with Left/Top/Right/Bottom properties and Zero static property</done>
</task>

<task type="auto">
  <name>Task 2: Add WindowRect.Empty property</name>
  <files>MatrixShader/src/MatrixShader.Core/Models/WindowInfo.cs</files>
  <action>
Add to existing WindowRect record:
```csharp
public static WindowRect Empty { get; } = new();
```

This provides a convenient zero-rectangle for error returns without creating new instances.
  </action>
  <verify>File compiles: `dotnet build MatrixShader/src/MatrixShader.Core`</verify>
  <done>WindowRect.Empty static property exists and returns zero-dimension rectangle</done>
</task>

<task type="auto">
  <name>Task 3: Add DWM P/Invoke declarations</name>
  <files>MatrixShader/src/MatrixShader.Core/Native/WindowsApi.cs</files>
  <action>
Add to WindowsApi.cs:

1. Add DWMWA constant in a new #region DWM Functions:
```csharp
private const int DWMWA_EXTENDED_FRAME_BOUNDS = 9;
```

2. Add DwmGetWindowAttribute P/Invoke using LibraryImport (AOT-compatible):
```csharp
[LibraryImport("dwmapi.dll")]
public static partial int DwmGetWindowAttribute(
    nint hwnd,
    int dwAttribute,
    out RECT pvAttribute,
    int cbAttribute);
```

3. Add Marshal.SizeOf import if not present (needed for cbAttribute).

Use LibraryImport, NOT DllImport, for Native AOT compatibility.
  </action>
  <verify>Build succeeds: `dotnet build MatrixShader/src/MatrixShader.Core`</verify>
  <done>DwmGetWindowAttribute declared with LibraryImport, DWMWA_EXTENDED_FRAME_BOUNDS constant defined</done>
</task>

</tasks>

<verification>
Run from project root:
```powershell
dotnet build MatrixShader/src/MatrixShader.Core
```

Expected: Build succeeds with no errors. May have existing warnings (not introduced by this plan).
</verification>

<success_criteria>
- BorderMargins.cs exists with record definition and Zero property
- WindowRect.Empty static property exists
- DwmGetWindowAttribute P/Invoke declared with LibraryImport attribute
- DWMWA_EXTENDED_FRAME_BOUNDS constant = 9
- Project builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/03-windows-api-layer/03-01-SUMMARY.md`
</output>
