---
phase: 11-installer-e2e-validation
plan: 07
type: execute
wave: 5
depends_on: ["11-06"]
files_modified:
  - installer/install.ps1
  - installer/uninstall.ps1
  - README.md
  - installer/TESTING.md
autonomous: true

must_haves:
  truths:
    - "One-liner install works: irm https://matrixshader.com/install.ps1 | iex"
    - "Script works with admin (Program Files) and without (LocalAppData)"
    - "Script is idempotent (safe to run twice)"
    - "Uninstall script removes everything cleanly"
    - "README shows one-liner as primary install method"
  artifacts:
    - path: "installer/install.ps1"
      provides: "One-liner install script"
      contains: "MatrixShader"
    - path: "installer/uninstall.ps1"
      provides: "Uninstall script"
      contains: "Remove"
    - path: "README.md"
      provides: "Updated install instructions"
      contains: "irm"
---

<objective>
Create PowerShell one-liner install script for frictionless installation.

Purpose: Enable `irm https://matrixshader.com/install.ps1 | iex` as primary install method. GUI installer remains as alternative.

Output: install.ps1 and uninstall.ps1 scripts, updated README with one-liner first.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create install.ps1 script</name>
  <files>installer/install.ps1</files>
  <action>
Create installer/install.ps1 that:

1. Detects if running as admin
2. Sets install location:
   - Admin: C:\Program Files\MatrixShader\
   - Non-admin: %LOCALAPPDATA%\Programs\MatrixShader\
3. Downloads latest release zip from GitHub Releases
4. Extracts all 6 executables to install location
5. Copies shaders to %LOCALAPPDATA%\MatrixShader\shaders\
6. Adds install location to PATH (system if admin, user if not)
7. Prints success message with next steps

Key features:
- Progress indicator during download
- Error handling with clear messages
- Idempotent (can run again to update)
- Works in both PowerShell 5.1 and 7+
  </action>
  <verify>Script syntax valid: powershell -Command "Get-Content installer/install.ps1 | Out-Null" returns no errors</verify>
  <done>install.ps1 created with admin/non-admin support</done>
</task>

<task type="auto">
  <name>Task 2: Create uninstall.ps1 script</name>
  <files>installer/uninstall.ps1</files>
  <action>
Create installer/uninstall.ps1 that:

1. Detects install location (Program Files or LocalAppData)
2. Removes executables
3. Removes PATH entry
4. Asks about user data (%LOCALAPPDATA%\MatrixShader\)
5. Prints confirmation message
  </action>
  <verify>Script syntax valid</verify>
  <done>uninstall.ps1 created</done>
</task>

<task type="auto">
  <name>Task 3: Verify README.md updated</name>
  <files>README.md</files>
  <action>
Verify README.md install section shows one-liner first (already updated by orchestrator).
  </action>
  <verify>grep -q "irm" README.md</verify>
  <done>README shows one-liner as primary install method</done>
</task>

<task type="auto">
  <name>Task 4: Verify TESTING.md updated</name>
  <files>installer/TESTING.md</files>
  <action>
Verify TESTING.md has one-liner test cases (already updated by orchestrator).
  </action>
  <verify>grep -q "E2E-00" installer/TESTING.md</verify>
  <done>TESTING.md has one-liner test cases</done>
</task>

</tasks>

<verification>
1. install.ps1 exists and has valid syntax
2. uninstall.ps1 exists and has valid syntax
3. README shows one-liner before GUI installer
4. TESTING.md has E2E-00 test cases
</verification>

<success_criteria>
- One-liner install script works with and without admin
- Uninstall script cleans up properly
- README prioritizes one-liner install
- Test checklist covers one-liner scenarios
</success_criteria>
