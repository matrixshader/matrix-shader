---
phase: 11-installer-e2e-validation
plan: 05
type: execute
wave: 3
depends_on: ["11-01", "11-02", "11-03", "11-04"]
files_modified:
  - README.md
  - installer/TESTING.md
autonomous: false

must_haves:
  truths:
    - "README has accurate install instructions (not npm)"
    - "Testing checklist exists for Windows Sandbox validation"
    - "User can follow checklist to validate installer"
  artifacts:
    - path: "README.md"
      provides: "Accurate user-facing install instructions"
      contains: "MatrixShaderSetup.exe"
    - path: "installer/TESTING.md"
      provides: "Manual testing checklist for Windows Sandbox"
      contains: "Windows Sandbox"
  key_links:
    - from: "README.md"
      to: "installer/output/MatrixShaderSetup.exe"
      via: "download instructions"
      pattern: "download.*installer"
---

<objective>
Update documentation and create testing checklist for Windows Sandbox validation.

Purpose: Closes GAP-E08 (README incorrect), GAP-E09 (no clean-system validation). Provides accurate user documentation and a checklist for manual E2E testing.

Output: Updated README with correct install instructions, and TESTING.md checklist for Windows Sandbox.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-installer-e2e-validation/11-RESEARCH.md
@.planning/phases/11-installer-e2e-validation/11-CONTEXT.md
@.planning/phases/11-installer-e2e-validation/GAP-ANALYSIS.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README with correct install instructions</name>
  <files>README.md</files>
  <action>
Rewrite README.md to provide accurate installation and usage instructions:

1. Remove incorrect npm install instructions

2. Update with correct installation section:
   ```markdown
   ## Installation

   ### Option 1: Installer (Recommended)

   1. Download `MatrixShaderSetup.exe` from the [Releases](../../releases) page
   2. Run the installer
   3. Open a **new** terminal window (required for PATH to update)
   4. Run `wakeupneo` to complete setup

   ### Option 2: Build from Source

   Requires: .NET 8 SDK, Windows 10/11

   ```powershell
   git clone https://github.com/matrixshader/matrix-shader.git
   cd matrix-shader
   dotnet build MatrixShader/MatrixShader.sln
   ```
   ```

3. Update commands section:
   ```markdown
   ## Commands

   After installation, these commands are available:

   | Command | Description |
   |---------|-------------|
   | `wakeupneo` | First-time setup wizard - creates profiles, configures shaders |
   | `bluepill` | Quick launch - restores previous session |
   | `redpill` | Control panel - full keyboard-driven TUI for parameter tweaking |
   | `matrixlite` | Text fallback - works in any terminal (no Windows Terminal required) |
   ```

4. Include matrix-hotkeys in the background process section (launched automatically by bluepill/redpill/wakeupneo)

5. Update requirements section:
   ```markdown
   ## Requirements

   - Windows 10 version 1903+ or Windows 11
   - Windows Terminal (installed automatically if missing)
   - GPU with DirectX 11+ support (for shader mode)

   **Note:** MatrixLite text mode works without Windows Terminal or GPU shaders.
   ```

6. Keep existing sections about features, architecture, etc. but review for accuracy
  </action>
  <verify>README.md exists and contains "MatrixShaderSetup" and no "npm install": `grep -c "MatrixShaderSetup" README.md` returns at least 1, and `grep -c "npm install" README.md` returns 0</verify>
  <done>README has accurate installation instructions and command reference</done>
</task>

<task type="auto">
  <name>Task 2: Create Windows Sandbox testing checklist</name>
  <files>installer/TESTING.md</files>
  <action>
Create installer/TESTING.md with a comprehensive manual testing checklist:

```markdown
# Matrix Shader Installer Testing Guide

Use Windows Sandbox to test the installer on a clean system.

## Prerequisites

- Windows 10 Pro/Enterprise or Windows 11 with Windows Sandbox enabled
- Built installer at `installer/output/MatrixShaderSetup.exe`

## Setup Windows Sandbox

1. Enable Windows Sandbox (Settings > Apps > Optional Features > More Windows features)
2. Create a shared folder for the installer
3. Copy MatrixShaderSetup.exe to the shared folder

## Testing Checklist

### E2E-01: Installation
- [ ] Installer runs without errors
- [ ] All 6 executables present in `C:\Program Files\MatrixShader\`:
  - [ ] wakeupneo.exe
  - [ ] bluepill.exe
  - [ ] redpill.exe
  - [ ] matrixlite.exe
  - [ ] matrix-hotkeys.exe
  - [ ] matrix-monitor.exe
- [ ] Shaders present in `C:\Program Files\MatrixShader\shaders\`
- [ ] Shaders copied to `%LOCALAPPDATA%\MatrixShader\shaders\`

### E2E-02: PATH Configuration
- [ ] Post-install message mentions "new terminal"
- [ ] Open NEW PowerShell window
- [ ] `where wakeupneo` shows correct path
- [ ] `where bluepill` shows correct path

### E2E-03: Windows Terminal Installation
- [ ] If WT not present, installer offers to install via winget
- [ ] After WT install, settings.json exists

### E2E-04: wakeupneo (First-Run)
- [ ] Run `wakeupneo` in new terminal
- [ ] Wizard completes without errors
- [ ] Matrix profiles created in Windows Terminal (check WT dropdown)
- [ ] Shader paths in profiles point to `%LOCALAPPDATA%\MatrixShader\shaders\`

### E2E-05: bluepill (Session Restore)
- [ ] Run `bluepill` in new terminal
- [ ] If no saved session, enters Lite mode gracefully
- [ ] If previous session exists, windows launch

### E2E-06: redpill (Control Panel)
- [ ] Run `redpill` in new terminal
- [ ] TUI displays correctly
- [ ] Can adjust shader parameters
- [ ] Changes reflect in shader windows

### E2E-07: matrixlite (Fallback Mode)
- [ ] Run `matrixlite` in CMD (not Windows Terminal)
- [ ] Text-based Matrix rain displays
- [ ] Colors work via ANSI codes
- [ ] Keyboard controls respond

### E2E-08: Uninstall
- [ ] Run uninstaller from Control Panel
- [ ] Executables removed from `C:\Program Files\MatrixShader\`
- [ ] `%LOCALAPPDATA%\MatrixShader\` removed
- [ ] PATH entry removed (check in new terminal)

## Windows Sandbox Config (.wsb)

Save as `MatrixShaderTest.wsb` and double-click to launch:

```xml
<Configuration>
  <VGpu>Enable</VGpu>
  <Networking>Enable</Networking>
  <MappedFolders>
    <MappedFolder>
      <HostFolder>C:\path\to\matrix\installer\output</HostFolder>
      <SandboxFolder>C:\Installer</SandboxFolder>
      <ReadOnly>true</ReadOnly>
    </MappedFolder>
  </MappedFolders>
</Configuration>
```

Update `HostFolder` to your actual path.

## Reporting Issues

If any check fails:
1. Note which step failed
2. Copy error messages
3. Check `%LOCALAPPDATA%\MatrixShader\debug.log` if MATRIX_DEBUG=1 was set
4. Report in GitHub issues with Windows version and WT version
```
  </action>
  <verify>installer/TESTING.md exists with E2E test coverage: `test -f installer/TESTING.md && grep -c "E2E-01" installer/TESTING.md` returns at least 1</verify>
  <done>Testing checklist created for Windows Sandbox E2E validation</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Updated README with accurate install instructions and created TESTING.md checklist for Windows Sandbox validation.
  </what-built>
  <how-to-verify>
1. Review README.md - verify it no longer mentions npm; matrix-hotkeys should be documented as background process
2. Review installer/TESTING.md - verify checklist covers all E2E scenarios
3. (Optional) If installer is built, test in Windows Sandbox following the checklist
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 11, or describe any documentation issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. README.md contains "MatrixShaderSetup" and no "npm install"
2. installer/TESTING.md exists with comprehensive checklist
3. User has verified documentation accuracy

Content verification for TESTING.md:
4. `grep -c "E2E-01" installer/TESTING.md` returns at least 1 (installation section exists)
5. `grep -c "E2E-08" installer/TESTING.md` returns at least 1 (uninstall section exists)
6. `grep -c "Windows Sandbox" installer/TESTING.md` returns at least 1 (sandbox instructions exist)
</verification>

<success_criteria>
- README has accurate installation instructions (no npm; documents matrix-hotkeys as background process)
- README documents all 4 commands correctly
- TESTING.md provides complete E2E testing checklist
- TESTING.md includes Windows Sandbox configuration
- TESTING.md covers all 8 E2E test scenarios (E2E-01 through E2E-08)
- User approves documentation
</success_criteria>

<output>
After completion, create `.planning/phases/11-installer-e2e-validation/11-05-SUMMARY.md`
</output>
