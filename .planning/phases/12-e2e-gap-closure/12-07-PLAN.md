---
phase: 12-e2e-gap-closure
plan: 07
type: execute
wave: 3
depends_on: ["12-01", "12-02", "12-03", "12-04", "12-05", "12-06"]
files_modified:
  - installer/output/MatrixShaderSetup.exe
autonomous: false

must_haves:
  truths:
    - "All 18 bugs from TESTING.md are fixed"
    - "Rebuilt installer passes full E2E test in Windows Sandbox"
    - "MatrixLite displays green in cmd.exe (not raw ANSI codes)"
    - "WT detection works for all install types"
    - "Uninstaller removes ALL files"
  artifacts:
    - path: "installer/output/MatrixShaderSetup.exe"
      provides: "Rebuilt installer with all fixes"
  key_links:
    - from: "MatrixShaderSetup.exe"
      to: "All fixed executables"
      via: "Installer packaging"
---

<objective>
Build installer and perform full E2E validation in Windows Sandbox

Purpose: Verify ALL 18 bugs are fixed before shipping. This is the final gate before release.

Output: Verified installer that passes all E2E tests
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-e2e-gap-closure/12-CONTEXT.md
@installer/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the installer with all fixes</name>
  <files>installer/output/MatrixShaderSetup.exe</files>
  <action>
Build the installer using the build script:

```powershell
cd C:\Users\ehome\Documents\Matrix
.\installer\build-installer.ps1
```

This will:
1. Publish all C# projects with self-contained runtime
2. Verify all 6 executables are present
3. Copy shaders from MatrixShader/shaders
4. Run Inno Setup to create the installer

Verify the output:
- installer/output/MatrixShaderSetup.exe exists
- Size is approximately 55MB (self-contained .NET runtime)

If build fails:
1. Check for compilation errors from Plans 12-01 through 12-06
2. Ensure Inno Setup 6.x is installed at C:\Program Files (x86)\Inno Setup 6\
3. Run builds individually to isolate issues:
   ```powershell
   dotnet build MatrixShader/MatrixShader.sln
   ```
  </action>
  <verify>
```powershell
Test-Path "C:\Users\ehome\Documents\Matrix\installer\output\MatrixShaderSetup.exe"
(Get-Item "C:\Users\ehome\Documents\Matrix\installer\output\MatrixShaderSetup.exe").Length / 1MB
```
  </verify>
  <done>Installer built successfully, ~55MB in size</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full E2E test in Windows Sandbox</name>
  <what-built>
Complete installer with all 18 bug fixes applied:
- Fixed shader regex replacement
- Fixed WT detection for all install types
- Fixed MatrixLite VT Processing for cmd.exe
- Fixed MatrixLite colors and menu
- Fixed uninstaller to remove all files
- Fixed first-run detection
- Added WT installation fallbacks
  </what-built>
  <how-to-verify>

**Setup Windows Sandbox:**
1. Double-click `MatrixShaderTest.wsb` (or create one pointing to installer/output)
2. Wait for sandbox to start

**Test E2E-01: GUI Installer**
- [ ] Run MatrixShaderSetup.exe from shared folder
- [ ] Installation completes without errors
- [ ] All 6 executables in C:\Program Files\MatrixShader:
  - wakeupneo.exe
  - bluepill.exe
  - redpill.exe
  - matrixlite.exe
  - matrix-hotkeys.exe
  - matrix-monitor.exe
- [ ] Shaders in %LOCALAPPDATA%\MatrixShader\shaders

**Test BUG-SHADER01: Shader generation**
- [ ] Run wakeupneo, create one window
- [ ] Check shader file: `type %LOCALAPPDATA%\MatrixShader\shaders\Matrix-1.hlsl | findstr "RAIN_R"`
- [ ] Should show `#define RAIN_R 0.0` NOT `$10.0`

**Test BUG-ML01/05/06: MatrixLite in cmd.exe**
- [ ] Open CMD (not PowerShell, not Windows Terminal)
- [ ] Run `matrixlite`
- [ ] See intro animation (not raw escape codes)
- [ ] See Red/Blue Pill choice
- [ ] Press B for Blue Pill - rain starts immediately
- [ ] Characters are GREEN (not blue, not all white)
- [ ] Head characters white, trail fades to dim green
- [ ] Press Q to return to menu

**Test BUG-WT01/02: WT Detection (without WT)**
- [ ] Run `wakeupneo` without WT installed
- [ ] Should detect WT missing and offer install options
- [ ] Should NOT silently fail or crash

**Test GAP-E03a/b/c: WT Installation flow**
- [ ] winget detection: Shows "winget not available" (not crash)
- [ ] Store option offered
- [ ] GitHub download option offered
- [ ] Manual instructions shown as last resort
- [ ] No dead ends - always shows a path forward

**Test BUG-FRX01: First-run detection**
- [ ] Fresh sandbox shows normal intro
- [ ] Does NOT show "Previous sessions found 8 window slots"

**Test GAP-INST01: Installer re-run**
- [ ] Run installer again (with previous install)
- [ ] See Update/Uninstall/Cancel dialog
- [ ] Select Update - proceeds with installation

**Test BUG-UNINST01/02: Uninstaller**
- [ ] Uninstall via Control Panel
- [ ] C:\Program Files\MatrixShader folder is EMPTY or GONE
- [ ] No 54+ DLLs left behind
- [ ] If any files remain, error message is actionable

**If Windows Terminal gets installed:**

**Test E2E-04: wakeupneo wizard**
- [ ] Run `wakeupneo`
- [ ] Wizard completes
- [ ] Matrix profiles created in WT

**Test E2E-05: bluepill session restore**
- [ ] Run `bluepill`
- [ ] Windows launch or Lite mode (if no session)

**Test E2E-06: redpill control panel**
- [ ] Run `redpill`
- [ ] TUI displays correctly

  </how-to-verify>
  <resume-signal>
Type "all tests passed" or describe failures:
- Which test failed
- What happened vs expected
- Error messages if any
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update TESTING.md with results</name>
  <files>installer/TESTING.md</files>
  <action>
After human verification, update TESTING.md with the Phase 12 test results:

Add new test session entry:

```markdown
## Test Session: 2026-XX-XX (Phase 12 Gap Closure Verification)

### Environment
- Windows Sandbox (minimal environment)
- Testing: Phase 12 bug fixes

### Bug Status

| Bug ID | Description | Status | Notes |
|--------|-------------|--------|-------|
| BUG-SHADER01 | Regex replacement bug | FIXED | #define shows correct values |
| BUG-ML01 | VT Processing missing | FIXED | ANSI codes work in cmd.exe |
| BUG-ML02 | Terminal blocked | FIXED | Menu mode allows navigation |
| BUG-ML03 | Missing background mode | FIXED | [B] key available |
| BUG-ML04 | Menu broken | FIXED | All keys respond |
| BUG-ML05 | Blue instead of green | FIXED | Classic Matrix green |
| BUG-ML06 | White characters | FIXED | Only heads are white |
| BUG-WT01 | WT detection broken | FIXED | Checks multiple paths |
| BUG-WT02 | Can't test WT apps | FIXED | Detection works |
| BUG-WT03 | Hardcoded wt.exe | FIXED | Dynamic discovery |
| BUG-WT04 | Wrong settings.json | FIXED | Multi-path resolution |
| GAP-E03a | No winget detection | FIXED | Checks before attempting |
| GAP-E03b | No download fallback | FIXED | GitHub option added |
| GAP-E03c | Dead end flow | FIXED | Always path forward |
| BUG-FRX01 | False previous sessions | FIXED | Checks state file |
| GAP-INST01 | Blind overwrite | FIXED | Re-run detection |
| BUG-UNINST01 | Useless error | FIXED | Actionable messages |
| BUG-UNINST02 | Files left behind | FIXED | Full cleanup |

### Phase 10 Skipped Tests (Now Performed)

| Test | Status | Notes |
|------|--------|-------|
| Visual Appearance Test | PASS | Green characters, white heads |
| Graceful Degradation Test | PASS | Falls back to Lite correctly |
| Terminal Resize Handling | PASS | Handles resize |
| Non-Windows-Terminal Compatibility | PASS | Works in cmd.exe |
| Standalone MatrixLite Operation | PASS | Full standalone experience |

### Conclusion
All 18 bugs fixed. Phase 12 complete.
```

Only add this after human confirms tests pass.
  </action>
  <verify>
Review TESTING.md for accurate status of all 18 bugs.
  </verify>
  <done>TESTING.md updated with Phase 12 verification results</done>
</task>

</tasks>

<verification>
1. Build completes without errors
2. Human verifies ALL checklist items in Windows Sandbox
3. TESTING.md updated with results
4. All 18 bugs marked FIXED
</verification>

<success_criteria>
From ROADMAP.md Phase 12 success criteria:
1. All 18 bugs from TESTING.md are fixed - VERIFIED
2. Local testing passes before sandbox rebuild - VERIFIED
3. Rebuilt installer passes full E2E test in Windows Sandbox - VERIFIED
4. matrixlite displays green in cmd.exe (not raw ANSI codes) - VERIFIED
5. WT detection works for Store, Scoop, Chocolatey, and portable installs - VERIFIED
6. Uninstaller removes ALL files from Program Files - VERIFIED
7. No dead ends in WT installation flow - VERIFIED
</success_criteria>

<output>
After completion, create `.planning/phases/12-e2e-gap-closure/12-07-SUMMARY.md`
</output>
