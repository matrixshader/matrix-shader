---
phase: 09-native-aot-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MatrixShader/src/MatrixShader.Core/MatrixShader.Core.csproj
  - MatrixShader/src/MatrixShader.Cli/Bluepill/MatrixShader.Cli.Bluepill.csproj
  - MatrixShader/src/MatrixShader.Cli/Redpill/MatrixShader.Cli.Redpill.csproj
  - MatrixShader/src/MatrixShader.Cli/WakeupNeo/MatrixShader.Cli.WakeupNeo.csproj
  - MatrixShader/src/MatrixShader.Monitor/MatrixShader.Monitor.csproj
  - MatrixShader/src/MatrixShader.Lite/MatrixShader.Lite.csproj
autonomous: true

must_haves:
  truths:
    - "All CLI projects publish to single-file Native AOT binaries"
    - "Monitor project publishes to single-file Native AOT binary"
    - "No trim warnings appear during publish"
    - "Published executables are self-contained (no runtime deps)"
  artifacts:
    - path: "MatrixShader/src/MatrixShader.Cli/Bluepill/MatrixShader.Cli.Bluepill.csproj"
      provides: "Native AOT publishing configuration"
      contains: "PublishSingleFile"
    - path: "MatrixShader/src/MatrixShader.Cli/Redpill/MatrixShader.Cli.Redpill.csproj"
      provides: "Native AOT publishing configuration"
      contains: "PublishSingleFile"
    - path: "MatrixShader/src/MatrixShader.Cli/WakeupNeo/MatrixShader.Cli.WakeupNeo.csproj"
      provides: "Native AOT publishing configuration"
      contains: "PublishSingleFile"
    - path: "MatrixShader/src/MatrixShader.Monitor/MatrixShader.Monitor.csproj"
      provides: "Native AOT publishing configuration"
      contains: "PublishSingleFile"
  key_links:
    - from: "CLI csproj files"
      to: "Core csproj"
      via: "ProjectReference"
      pattern: "ProjectReference.*MatrixShader\\.Core"
    - from: "Monitor csproj"
      to: "Core csproj"
      via: "ProjectReference"
      pattern: "ProjectReference.*MatrixShader\\.Core"
---

<objective>
Configure all CLI projects AND the Monitor project for Native AOT single-file publishing with proper trimming settings.

Purpose: Enable dotnet publish to produce self-contained single-file executables without .NET runtime dependency
Output: All four executables (bluepill, redpill, wakeupneo, matrix-monitor) compile to native binaries ready for distribution
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-native-aot-polish/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLI and Monitor csproj files with complete AOT properties</name>
  <files>
    MatrixShader/src/MatrixShader.Cli/Bluepill/MatrixShader.Cli.Bluepill.csproj
    MatrixShader/src/MatrixShader.Cli/Redpill/MatrixShader.Cli.Redpill.csproj
    MatrixShader/src/MatrixShader.Cli/WakeupNeo/MatrixShader.Cli.WakeupNeo.csproj
    MatrixShader/src/MatrixShader.Monitor/MatrixShader.Monitor.csproj
  </files>
  <action>
    Add the following properties to each CLI and Monitor csproj's PropertyGroup (some already exist, add missing ones):

    ```xml
    <!-- Native AOT Configuration -->
    <PublishAot>true</PublishAot>
    <PublishSingleFile>true</PublishSingleFile>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <SelfContained>true</SelfContained>
    <InvariantGlobalization>true</InvariantGlobalization>
    <IlcOptimizationPreference>Size</IlcOptimizationPreference>
    ```

    For CLI projects - Existing properties: PublishAot, InvariantGlobalization, IlcOptimizationPreference are already present.
    Add: PublishSingleFile, RuntimeIdentifier, SelfContained.

    For Monitor project - Existing properties: PublishAot, InvariantGlobalization, IlcOptimizationPreference are already present.
    Add: PublishSingleFile, RuntimeIdentifier, SelfContained.

    Keep the existing TreatWarningsAsErrors or NoWarn settings if present. Do NOT suppress IL2XXX warnings.
  </action>
  <verify>
    Read each csproj file and confirm all 6 properties are present in PropertyGroup.
  </verify>
  <done>
    All four executable csproj files (three CLIs + Monitor) contain: PublishAot=true, PublishSingleFile=true, RuntimeIdentifier=win-x64, SelfContained=true, InvariantGlobalization=true, IlcOptimizationPreference=Size
  </done>
</task>

<task type="auto">
  <name>Task 2: Update library csproj files for AOT compatibility</name>
  <files>
    MatrixShader/src/MatrixShader.Core/MatrixShader.Core.csproj
    MatrixShader/src/MatrixShader.Lite/MatrixShader.Lite.csproj
  </files>
  <action>
    Add IsAotCompatible property to Core and Lite library csproj files:

    ```xml
    <IsAotCompatible>true</IsAotCompatible>
    ```

    This enables the AOT compatibility analyzer to catch issues during library compilation rather than waiting for publish.

    For Core, keep the existing JsonSerializerIsReflectionEnabledByDefault=false property.
  </action>
  <verify>
    Read each library csproj and confirm IsAotCompatible is present.
  </verify>
  <done>
    Both MatrixShader.Core.csproj and MatrixShader.Lite.csproj contain IsAotCompatible=true
  </done>
</task>

<task type="auto">
  <name>Task 3: Test Native AOT publish and verify single-file output</name>
  <files>None (verification only)</files>
  <action>
    Run dotnet publish for all four executable projects in Release mode:

    ```bash
    cd MatrixShader
    dotnet publish src/MatrixShader.Cli/Bluepill -c Release
    dotnet publish src/MatrixShader.Cli/Redpill -c Release
    dotnet publish src/MatrixShader.Cli/WakeupNeo -c Release
    dotnet publish src/MatrixShader.Monitor -c Release
    ```

    Verify:
    1. No IL2XXX trim warnings in output
    2. Each produces a single .exe file (no .dll files alongside)
    3. Executables are in the expected publish folder

    Check publish output directories:
    - src/MatrixShader.Cli/Bluepill/bin/Release/net8.0-windows/win-x64/publish/
    - src/MatrixShader.Cli/Redpill/bin/Release/net8.0-windows/win-x64/publish/
    - src/MatrixShader.Cli/WakeupNeo/bin/Release/net8.0-windows/win-x64/publish/
    - src/MatrixShader.Monitor/bin/Release/net8.0-windows/win-x64/publish/

    If warnings appear, document them but do not suppress - they need to be fixed properly.
  </action>
  <verify>
    List files in each publish directory. Should contain only the .exe file (and possibly .pdb for debugging).
    Run: `dir /b path\to\publish\` for each.
  </verify>
  <done>
    All four executables (three CLIs + matrix-monitor) publish successfully to single-file executables with no IL2XXX warnings.
  </done>
</task>

</tasks>

<verification>
- [ ] bluepill.csproj has all 6 AOT properties
- [ ] redpill.csproj has all 6 AOT properties
- [ ] wakeupneo.csproj has all 6 AOT properties
- [ ] Monitor.csproj has all 6 AOT properties
- [ ] Core.csproj has IsAotCompatible=true
- [ ] Lite.csproj has IsAotCompatible=true
- [ ] `dotnet publish` completes without IL2XXX warnings
- [ ] Each publish folder contains single .exe file
</verification>

<success_criteria>
All four executables (bluepill.exe, redpill.exe, wakeupneo.exe, matrix-monitor.exe) compile to single-file Native AOT binaries with no trim warnings.
</success_criteria>

<output>
After completion, create `.planning/phases/09-native-aot-polish/09-01-SUMMARY.md`
</output>
