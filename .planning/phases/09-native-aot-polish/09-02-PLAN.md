---
phase: 09-native-aot-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - MatrixShader/src/MatrixShader.Core/Startup/MatrixSplash.cs
  - MatrixShader/src/MatrixShader.Core/Startup/MatrixErrorHandler.cs
autonomous: true

must_haves:
  truths:
    - "Matrix number cascade animation displays on startup"
    - "Animation runs for minimum 1.5 seconds or until app ready"
    - "Error handler displays themed 'SYSTEM FAILURE' message"
    - "Error experience waits for keypress before closing"
  artifacts:
    - path: "MatrixShader/src/MatrixShader.Core/Startup/MatrixSplash.cs"
      provides: "Matrix number cascade animation"
      exports: ["MatrixSplash", "ShowAsync"]
      min_lines: 50
    - path: "MatrixShader/src/MatrixShader.Core/Startup/MatrixErrorHandler.cs"
      provides: "Themed error experience"
      exports: ["MatrixErrorHandler", "ShowError"]
      min_lines: 40
  key_links:
    - from: "MatrixSplash"
      to: "Console"
      via: "ANSI escape codes"
      pattern: "\\x1b\\["
---

<objective>
Create Matrix-themed startup splash animation and error handler in Core library.

Purpose: Deliver late-90s telnet hacker aesthetic with cascading Matrix numbers on startup and themed error experience
Output: Shared components in Core that all CLIs can use for consistent branding
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-native-aot-polish/09-CONTEXT.md
@.planning/phases/09-native-aot-polish/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MatrixSplash with cascading number animation</name>
  <files>MatrixShader/src/MatrixShader.Core/Startup/MatrixSplash.cs</files>
  <action>
    Create new Startup folder in Core and add MatrixSplash.cs.

    Implement Matrix movie-style cascading green numbers:
    - Characters: 0-9 plus some Katakana-style symbols or uppercase letters (A-Z)
    - Lead character is bright white (\x1b[97m), trail fades to green (\x1b[32m)
    - Multiple columns falling at different speeds
    - Duration: configurable, default 1500ms minimum
    - Use Stopwatch.GetTimestamp/GetElapsedTime for timing (monotonic, high-res)
    - Clear screen before and after animation
    - Hide cursor during animation

    Pattern from research:
    ```csharp
    public static class MatrixSplash
    {
        private static readonly Random _rng = new();
        private static readonly char[] _chars = "0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ".ToCharArray();

        public static async Task ShowAsync(int durationMs = 1500)
        {
            Console.Clear();
            Console.CursorVisible = false;

            var width = Console.WindowWidth;
            var height = Console.WindowHeight;
            var startTime = Stopwatch.GetTimestamp();

            // Column drop positions (staggered start)
            var columns = new int[width];
            for (int i = 0; i < width; i++)
                columns[i] = _rng.Next(-height, 0);

            while (Stopwatch.GetElapsedTime(startTime).TotalMilliseconds < durationMs)
            {
                // Update and render columns
                // ...
                await Task.Delay(50); // ~20 FPS
            }

            Console.Clear();
        }
    }
    ```

    Make it visually impressive but simple. Each column:
    1. Has a lead character (bright white)
    2. Has a trail of green characters that fade
    3. Resets to top when reaching bottom

    Use try/finally to restore cursor visibility.
  </action>
  <verify>
    Read the file to confirm it compiles. The animation behavior will be verified during integration (Plan 3).
  </verify>
  <done>
    MatrixSplash.cs exists with ShowAsync method that displays cascading green numbers for specified duration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MatrixErrorHandler with themed error display</name>
  <files>MatrixShader/src/MatrixShader.Core/Startup/MatrixErrorHandler.cs</files>
  <action>
    Create MatrixErrorHandler.cs in Startup folder.

    Implement Matrix-themed error display:
    - ASCII art "SYSTEM FAILURE" banner (red text on black)
    - Error message in green text (Matrix style)
    - Optional action URL with "Jack in at:" prefix
    - Wait for keypress before exiting
    - Late 90s telnet hacker group aesthetic

    Pattern from research:
    ```csharp
    public static class MatrixErrorHandler
    {
        public static void ShowError(string message, string? actionUrl = null)
        {
            Console.Clear();
            Console.WriteLine();

            // SYSTEM FAILURE ASCII art in red
            Console.WriteLine("\x1b[31m ███████╗██╗   ██╗███████╗████████╗███████╗███╗   ███╗\x1b[0m");
            Console.WriteLine("\x1b[31m ██╔════╝╚██╗ ██╔╝██╔════╝╚══██╔══╝██╔════╝████╗ ████║\x1b[0m");
            // ... more ASCII art rows

            Console.WriteLine();
            Console.WriteLine($"\x1b[32m > {message}\x1b[0m");
            Console.WriteLine();

            if (!string.IsNullOrEmpty(actionUrl))
            {
                Console.WriteLine($"\x1b[90m Jack in at: {actionUrl}\x1b[0m");
                Console.WriteLine();
            }

            Console.WriteLine("\x1b[90m Press any key to exit the simulation...\x1b[0m");
            Console.ReadKey(intercept: true);
        }
    }
    ```

    Use box-drawing or block characters for the ASCII art. Keep it readable in standard terminal fonts.
    The research shows a specific style - adapt it to ensure it renders correctly in Windows Terminal.
  </action>
  <verify>
    Read the file to confirm ShowError method exists with message and optional actionUrl parameters.
  </verify>
  <done>
    MatrixErrorHandler.cs exists with ShowError method that displays themed error banner and waits for keypress.
  </done>
</task>

</tasks>

<verification>
- [ ] MatrixShader/src/MatrixShader.Core/Startup/ folder exists
- [ ] MatrixSplash.cs compiles and exports ShowAsync method
- [ ] MatrixErrorHandler.cs compiles and exports ShowError method
- [ ] Both use ANSI escape codes for colors (no external dependencies)
- [ ] MatrixSplash uses Stopwatch for timing (not DateTime)
</verification>

<success_criteria>
Core library has MatrixSplash and MatrixErrorHandler static classes ready for integration into CLI entry points.
</success_criteria>

<output>
After completion, create `.planning/phases/09-native-aot-polish/09-02-SUMMARY.md`
</output>
