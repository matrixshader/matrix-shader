---
phase: 09-native-aot-polish
plan: 03
type: execute
wave: 2
depends_on:
  - "09-02"
files_modified:
  - MatrixShader/src/MatrixShader.Cli/Bluepill/Program.cs
  - MatrixShader/src/MatrixShader.Cli/Redpill/Program.cs
  - MatrixShader/src/MatrixShader.Cli/WakeupNeo/Program.cs
autonomous: true

must_haves:
  truths:
    - "All three CLIs show Matrix splash on startup"
    - "Splash appears before any DI setup or service initialization"
    - "Unhandled exceptions display themed error experience"
    - "--help flag skips splash animation"
  artifacts:
    - path: "MatrixShader/src/MatrixShader.Cli/Bluepill/Program.cs"
      provides: "Splash integration"
      contains: "MatrixSplash.ShowAsync"
    - path: "MatrixShader/src/MatrixShader.Cli/Redpill/Program.cs"
      provides: "Splash integration"
      contains: "MatrixSplash.ShowAsync"
    - path: "MatrixShader/src/MatrixShader.Cli/WakeupNeo/Program.cs"
      provides: "Splash integration"
      contains: "MatrixSplash.ShowAsync"
  key_links:
    - from: "Program.Main"
      to: "MatrixSplash"
      via: "await ShowAsync at entry"
      pattern: "MatrixSplash\\.ShowAsync"
    - from: "catch block"
      to: "MatrixErrorHandler"
      via: "ShowError call"
      pattern: "MatrixErrorHandler\\.ShowError"
---

<objective>
Integrate Matrix splash and error handler into all three CLI entry points.

Purpose: Ensure consistent branded experience across all executables with splash animation before DI setup
Output: All CLIs show Matrix cascade on startup and themed errors on failure
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-native-aot-polish/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add splash to Bluepill entry point</name>
  <files>MatrixShader/src/MatrixShader.Cli/Bluepill/Program.cs</files>
  <action>
    Modify Program.Main in Bluepill to:

    1. Add using statement: `using MatrixShader.Core.Startup;`

    2. At the VERY START of Main, before ParseArgs:
       - Check if --help is in args (quick check, skip splash for help)
       - If not help, call `await MatrixSplash.ShowAsync();`

    3. Wrap entire Main body in try-catch:
       - Catch Exception ex
       - Call `MatrixErrorHandler.ShowError(ex.Message);`
       - Return 1

    Pattern:
    ```csharp
    public static async Task<int> Main(string[] args)
    {
        // Skip splash for help
        if (!args.Contains("--help"))
        {
            await MatrixSplash.ShowAsync();
        }

        try
        {
            // Parse arguments
            var options = CliBootstrap.ParseArgs(args);
            // ... rest of existing code ...
        }
        catch (Exception ex)
        {
            MatrixErrorHandler.ShowError(ex.Message);
            return 1;
        }
    }
    ```

    Keep the existing ConsoleHelper.WriteLineMatrixGreen error handling for bootstrap failures - only use MatrixErrorHandler for unexpected exceptions.
  </action>
  <verify>
    Read Bluepill/Program.cs and confirm MatrixSplash.ShowAsync is called at top of Main, and MatrixErrorHandler.ShowError is in catch block.
  </verify>
  <done>
    Bluepill shows Matrix splash on startup and themed errors on unhandled exceptions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add splash to Redpill entry point</name>
  <files>MatrixShader/src/MatrixShader.Cli/Redpill/Program.cs</files>
  <action>
    Same pattern as Task 1 for Redpill:

    1. Add using statement: `using MatrixShader.Core.Startup;`

    2. At VERY START of Main, before ParseArgs:
       - Skip splash for --help
       - Call `await MatrixSplash.ShowAsync();`

    3. Wrap entire Main body in try-catch with MatrixErrorHandler

    Redpill has existing exception handling with logger - keep the logger.LogError but add MatrixErrorHandler for user-visible error experience before returning error code.
  </action>
  <verify>
    Read Redpill/Program.cs and confirm MatrixSplash.ShowAsync and MatrixErrorHandler integration.
  </verify>
  <done>
    Redpill shows Matrix splash on startup and themed errors on unhandled exceptions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add splash to WakeupNeo entry point</name>
  <files>MatrixShader/src/MatrixShader.Cli/WakeupNeo/Program.cs</files>
  <action>
    Same pattern as Task 1 for WakeupNeo:

    1. Add using statement: `using MatrixShader.Core.Startup;`

    2. At VERY START of Main, before ParseArgs:
       - Skip splash for --help
       - Call `await MatrixSplash.ShowAsync();`

    3. Wrap entire Main body in try-catch with MatrixErrorHandler

    Note: WakeupNeo already has a dramatic intro ("Wake up, Neo...") - the splash animation will precede this. The splash is the branded startup, then the wizard does its own dramatic typing.
  </action>
  <verify>
    Read WakeupNeo/Program.cs and confirm MatrixSplash.ShowAsync and MatrixErrorHandler integration.
  </verify>
  <done>
    WakeupNeo shows Matrix splash on startup and themed errors on unhandled exceptions.
  </done>
</task>

</tasks>

<verification>
- [ ] Bluepill: MatrixSplash.ShowAsync called before ParseArgs
- [ ] Bluepill: MatrixErrorHandler.ShowError in catch block
- [ ] Redpill: MatrixSplash.ShowAsync called before ParseArgs
- [ ] Redpill: MatrixErrorHandler.ShowError in catch block
- [ ] WakeupNeo: MatrixSplash.ShowAsync called before ParseArgs
- [ ] WakeupNeo: MatrixErrorHandler.ShowError in catch block
- [ ] All three skip splash when --help is passed
- [ ] `dotnet build` succeeds for all CLI projects
</verification>

<success_criteria>
All three CLI executables display Matrix splash animation on startup (unless --help) and show themed error messages on unexpected failures.
</success_criteria>

<output>
After completion, create `.planning/phases/09-native-aot-polish/09-03-SUMMARY.md`
</output>
