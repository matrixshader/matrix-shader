---
phase: 14-final-polish-hotkey-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MatrixShader/src/MatrixShader.Hotkeys/Program.cs
  - MatrixShader/src/MatrixShader.Hotkeys/MatrixWindowMonitor.cs
  - MatrixShader/src/MatrixShader.Monitor/Program.cs
autonomous: true

must_haves:
  truths:
    - "Hotkeys remain functional indefinitely without redpill open"
    - "matrix-hotkeys.exe auto-restarts when it crashes"
    - "Hotkey service stays alive 30 seconds after last Matrix window closes"
    - "Glitch/snap works without redpill being open"
  artifacts:
    - path: "MatrixShader/src/MatrixShader.Hotkeys/Program.cs"
      provides: "Top-level exception handler, stay-alive timer"
      contains: "AppDomain.CurrentDomain.UnhandledException"
    - path: "MatrixShader/src/MatrixShader.Hotkeys/MatrixWindowMonitor.cs"
      provides: "30-second stay-alive logic"
      contains: "_lastWindowSeen"
    - path: "MatrixShader/src/MatrixShader.Monitor/Program.cs"
      provides: "Hotkey process supervision"
      contains: "HotkeyWatchdog"
  key_links:
    - from: "MatrixShader.Monitor/Program.cs"
      to: "matrix-hotkeys.exe"
      via: "Process.Start with health check timer"
      pattern: "Process\\.Start.*matrix-hotkeys"
    - from: "MatrixWindowMonitor.cs"
      to: "_onNoWindows"
      via: "30-second stay-alive delay"
      pattern: "_lastWindowSeen.*StayAliveSeconds"
---

<objective>
Fix critical hotkey service stability bugs (BUG-HK01, BUG-HK04).

Purpose: Hotkeys stop working after 1-2 uses because matrix-hotkeys.exe crashes silently. The glitch/snap system also requires redpill to be open, but should run in the background.

Output: matrix-hotkeys.exe that stays alive and auto-restarts on crash, with glitch/snap working independently of redpill.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-final-polish-hotkey-stability/14-CONTEXT.md
@.planning/phases/14-final-polish-hotkey-stability/14-RESEARCH.md
@MatrixShader/src/MatrixShader.Hotkeys/Program.cs
@MatrixShader/src/MatrixShader.Hotkeys/MatrixWindowMonitor.cs
@MatrixShader/src/MatrixShader.Monitor/Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add top-level exception handling and stay-alive timer to matrix-hotkeys</name>
  <files>MatrixShader/src/MatrixShader.Hotkeys/Program.cs, MatrixShader/src/MatrixShader.Hotkeys/MatrixWindowMonitor.cs</files>
  <action>
    In Program.cs:
    1. Add `AppDomain.CurrentDomain.UnhandledException += (s, e) => DiagnosticLogger.Error("HOTKEYS", $"Unhandled: {e.ExceptionObject}");` before the Run() call
    2. Wrap the entire Run() in a try-catch that logs and continues if possible

    In MatrixWindowMonitor.cs:
    1. Add `private DateTime _lastWindowSeen = DateTime.Now;` field
    2. Add `private const int StayAliveSeconds = 30;` constant
    3. Modify CheckWindows() to track _lastWindowSeen when windows exist
    4. When no windows found, check if 30 seconds elapsed since _lastWindowSeen before calling _onNoWindows()
    5. Log the countdown: "No windows, stay-alive: {remaining}s"

    This ensures the hotkey service doesn't exit immediately when Matrix windows close, giving users time to reopen them.
  </action>
  <verify>Build succeeds: `dotnet build MatrixShader/src/MatrixShader.Hotkeys/`</verify>
  <done>matrix-hotkeys.exe has exception handling and waits 30 seconds before exiting when no windows</done>
</task>

<task type="auto">
  <name>Task 2: Add hotkey process watchdog to matrix-monitor</name>
  <files>MatrixShader/src/MatrixShader.Monitor/Program.cs</files>
  <action>
    Add a HotkeyWatchdog class to Program.cs (or new file) that:
    1. Takes the path to matrix-hotkeys.exe in constructor
    2. Uses a Timer (5-second interval) to check if process is running via Process.HasExited
    3. If process is null or has exited, starts it with CreateNoWindow = true, UseShellExecute = false
    4. Logs restarts: "Hotkey process started: PID {id}" or "Hotkey process restarted"

    In MonitorService.ExecuteAsync:
    1. Find matrix-hotkeys.exe path (same directory as matrix-monitor.exe, or installed location)
    2. Create HotkeyWatchdog instance at start
    3. Dispose watchdog on cancellation

    Path resolution:
    - Try `Path.Combine(AppContext.BaseDirectory, "matrix-hotkeys.exe")` first
    - Fall back to `Path.Combine(Environment.GetFolderPath(SpecialFolder.ProgramFiles), "MatrixShader", "matrix-hotkeys.exe")`

    The watchdog ensures hotkey process auto-restarts if it crashes.
  </action>
  <verify>Build succeeds: `dotnet build MatrixShader/src/MatrixShader.Monitor/`</verify>
  <done>matrix-monitor.exe supervises and auto-restarts matrix-hotkeys.exe on crash</done>
</task>

</tasks>

<verification>
1. Build both projects: `dotnet build MatrixShader/MatrixShader.sln`
2. Verify no compiler errors
3. Check DiagnosticLogger calls are present for debugging
</verification>

<success_criteria>
- matrix-hotkeys.exe has top-level exception handler
- Stay-alive timer prevents immediate exit (30 seconds)
- matrix-monitor.exe includes HotkeyWatchdog
- Watchdog uses 5-second health check interval
- All builds pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-final-polish-hotkey-stability/14-01-SUMMARY.md`
</output>
