---
phase: 14-final-polish-hotkey-stability
plan: 05
type: execute
wave: 4
depends_on: [14-03]
files_modified:
  - MatrixShader/src/MatrixShader.Hotkeys/HotkeyActions.cs
  - MatrixShader/src/MatrixShader.Core/Models/HotkeyConfig.cs
  - MatrixShader/src/MatrixShader.Core/Models/HotkeyAction.cs
  - MatrixShader/src/MatrixShader.Core/Constants/ColorPresets.cs
autonomous: true

must_haves:
  truths:
    - "Shader cycling feature (Ctrl+Shift+S) is completely removed"
    - "No CycleShader hotkey registration or action"
    - "Color preset 'Cyan' is renamed to 'Blue'"
  artifacts:
    - path: "MatrixShader/src/MatrixShader.Hotkeys/HotkeyActions.cs"
      provides: "CycleShader method removed"
      excludes: "CycleShader"
    - path: "MatrixShader/src/MatrixShader.Core/Models/HotkeyConfig.cs"
      provides: "No CycleShader binding in defaults"
      excludes: "HotkeyAction.CycleShader"
    - path: "MatrixShader/src/MatrixShader.Core/Constants/ColorPresets.cs"
      provides: "Blue color preset (renamed from Cyan)"
      contains: "Blue = new"
  key_links:
    - from: "HotkeyConfig.DefaultBindings"
      to: "HotkeyAction enum"
      via: "Excludes CycleShader"
      pattern: "!CycleShader"
---

<objective>
Remove shader cycling and rename color preset (BUG-SHADER04, BUG-SHADER05, UX-COLOR01).

Purpose: Per user decision, shader cycling is being REMOVED entirely because it corrupts shader colors/parameters and shaders only differ by color anyway. Also rename "Cyan" to "Blue" for user clarity.

Output: CycleShader hotkey and action removed, Cyan renamed to Blue.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-final-polish-hotkey-stability/14-CONTEXT.md
@.planning/phases/14-final-polish-hotkey-stability/14-RESEARCH.md
@MatrixShader/src/MatrixShader.Hotkeys/HotkeyActions.cs
@MatrixShader/src/MatrixShader.Core/Models/HotkeyConfig.cs
@MatrixShader/src/MatrixShader.Core/Models/HotkeyAction.cs
@MatrixShader/src/MatrixShader.Core/Constants/ColorPresets.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove shader cycling feature</name>
  <files>MatrixShader/src/MatrixShader.Hotkeys/HotkeyActions.cs, MatrixShader/src/MatrixShader.Core/Models/HotkeyConfig.cs, MatrixShader/src/MatrixShader.Core/Models/HotkeyAction.cs</files>
  <action>
    1. In HotkeyActions.cs:
       - Remove the CycleShader method (around lines 269-299)
       - Remove the GetNextShaderIndex helper method (around lines 305-321)
       - Remove the `HotkeyAction.CycleShader => CycleShader,` line from GetHandler switch (around line 60)
       - Remove the shader cycling constants (MinShaderIndex, MaxShaderIndex) if only used by CycleShader

    2. In HotkeyConfig.cs:
       - Remove the CycleShader binding from DefaultBindings() (around line 31):
         Delete: `[HotkeyAction.CycleShader] = HotkeyBinding.CtrlShift(HotkeyAction.CycleShader, HotkeyApi.VK_S, "S"),`

    3. In HotkeyAction.cs (if it's an enum):
       - Comment out or remove `CycleShader` from the enum
       - Note: If removing would break JSON deserialization of existing configs, just comment it out with // REMOVED

    After these changes:
    - Ctrl+Shift+S will not be registered as a hotkey
    - No shader cycling will occur
    - Existing hotkey config files with CycleShader will be ignored (since it's not in DefaultBindings)
  </action>
  <verify>Build succeeds: `dotnet build MatrixShader/src/MatrixShader.Hotkeys/`</verify>
  <done>Shader cycling feature completely removed from codebase</done>
</task>

<task type="auto">
  <name>Task 2: Rename Cyan color preset to Blue</name>
  <files>MatrixShader/src/MatrixShader.Core/Constants/ColorPresets.cs</files>
  <action>
    In ColorPresets.cs (around line 12):

    1. Change:
       `public static readonly MatrixColor Cyan = new(0f, 0.6f, 1f, "Cyan", "Electric blue");`
       to:
       `public static readonly MatrixColor Blue = new(0f, 0.6f, 1f, "Blue", "Electric blue");`

    2. Update the All array (around line 29):
       Change:
       `public static readonly MatrixColor[] All = [Green, Cyan, Red, Purple, Gold, Teal];`
       to:
       `public static readonly MatrixColor[] All = [Green, Blue, Red, Purple, Gold, Teal];`

    3. Search for any other references to "Cyan" in the codebase and update:
       - `grep -r "Cyan" MatrixShader/src/` to find any hardcoded references
       - The GetByName method will automatically work with "Blue" since it reads from the Name property

    Note: The RGB values (0f, 0.6f, 1f) stay the same - it's still the same color, just renamed.
  </action>
  <verify>Build succeeds: `dotnet build MatrixShader/src/MatrixShader.Core/`</verify>
  <done>Cyan color preset renamed to Blue</done>
</task>

</tasks>

<verification>
1. Build all projects: `dotnet build MatrixShader/MatrixShader.sln`
2. Verify CycleShader is not in HotkeyConfig.DefaultBindings
3. Verify no CycleShader handler in HotkeyActions.GetHandler
4. Verify ColorPresets.Blue exists with correct RGB values
5. Verify no references to "Cyan" remain (except comments)
</verification>

<success_criteria>
- CycleShader method removed from HotkeyActions.cs
- CycleShader binding removed from HotkeyConfig.DefaultBindings
- Ctrl+Shift+S no longer registered as hotkey
- ColorPresets.Cyan renamed to ColorPresets.Blue
- All array updated to include Blue instead of Cyan
- All builds pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-final-polish-hotkey-stability/14-05-SUMMARY.md`
</output>
